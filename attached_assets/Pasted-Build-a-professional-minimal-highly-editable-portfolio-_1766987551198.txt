Build a professional, minimal, highly-editable portfolio website using Next.js (App Router) and TypeScript.

GOALS:
- Create a complete portfolio “shell” with placeholder content only. No real project content yet.
- The site must be data-driven so I can add/edit/delete projects by editing a single file later.
- Clean, readable UI. No flashy animations. Simple and credible.

PAGES (App Router):
1) Home: Hero, short intro, two sections (Games + Software), and a CTA to Projects.
2) Projects: Show Games and Software separately (either tabs or two sections). Use ProjectCard components.
3) Project Detail: /projects/[slug] auto-generates pages from the project data file.
4) About: Placeholder “about” text pulled from a profile data file.
5) Resume: Placeholder section with a button/link. I will add a PDF later.
6) Contact: Simple contact + social links pulled from profile data.

DATA-DRIVEN REQUIREMENTS:
- Create /src/data/profile.ts exporting name, headline, short bio, email, GitHub, LinkedIn.
- Create /src/data/projects.ts exporting an array of projects.
- Each project should include:
  slug, title, category ("Game" | "Software"), status, shortPurpose, systemsBullets (string[]),
  tech (string[]), links { github?, demo?, video? } optional.

COMPONENTS:
- Navbar + Footer
- ProjectCard
- ProjectSections for Games/Software
- Tag/Badge component

STYLING:
- Use Tailwind CSS for styling.
- Typography should be readable, lots of whitespace, responsive design.
- Dark mode optional but not required.

PLACEHOLDER CONTENT:
- Include placeholder projects with these slugs so the structure is ready:
  legends-of-alden-1, legends-of-alden-2, legends-of-alden-3, heist-gone-wrong, abet-portal
- Use generic placeholder descriptions and bullets (systems-focused), not real details.

EXTRA:
- Add a simple “Edit content here” comment in profile.ts and projects.ts.
- Make sure the site builds and runs with `npm run dev`.

Return the full working code.
