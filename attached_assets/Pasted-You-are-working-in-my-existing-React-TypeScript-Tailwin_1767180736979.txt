You are working in my existing React + TypeScript + Tailwind portfolio codebase in Replit.

GOALS
1) Add a “Media” section to every Project Detail page (the /projects/:slug route) that supports:
   - A primary featured media item (image OR video)
   - An optional gallery grid of additional media items (images and/or videos)
   - Clicking a thumbnail opens a simple modal/lightbox to view it larger
   - The system must make it EASY for me to add media later by only editing the project data (not the UI code)

2) Projects page: make the filter buttons/pills (“All / Games / Software”) noticeably bigger and less empty:
   - Increase padding, font-size, and border radius slightly
   - Add a subtle background for inactive pills and stronger active styling
   - Ensure spacing between pills looks intentional (not tiny)

3) Home page: change the “View My Work” button to a grey style:
   - Medium grey background, white text, slightly darker grey on hover
   - Keep it consistent with the rest of the site

IMPLEMENTATION DETAILS
A) DATA MODEL CHANGE (single source of truth)
- In the project data file (likely client/src/data/projects.ts or similar), add:
  - featuredMedia?: MediaItem
  - media?: MediaItem[]
- Define a MediaItem type:
  type MediaItem = {
    type: "image" | "video";
    src: string;            // for images: URL/path; for video: URL (mp4) or embed URL
    poster?: string;        // optional poster for videos
    alt?: string;           // for images
    caption?: string;
  }

- For now, add placeholder media entries for 1–2 projects so the UI is testable.
  Use safe placeholders like:
  - image: https://placehold.co/1200x675/png
  - video: (optional) just include one mp4 placeholder or skip video if not available

B) PROJECT DETAIL UI
- In ProjectDetail page component, insert a new “Media” section directly under title/status and above “Purpose”.
- Featured media:
  - If featuredMedia exists, render it large (16:9 container).
  - If type === "image": render <img> with object-cover, rounded corners.
  - If type === "video": render <video controls> (or iframe if it’s an embed URL – but prefer <video> if src ends with .mp4).
- Gallery:
  - If media array exists and has items, render a responsive grid (2 columns on small, 3 on md+).
  - Each item renders a thumbnail card; clicking opens modal/lightbox.
  - Modal includes close button, ESC closes, click backdrop closes.
  - Modal displays caption if provided.

C) STYLING / TAILWIND
- Keep consistent spacing with existing sections (use same max width and typography).
- Media section should look clean: title “Media”, then featured, then thumbnails.
- Thumbnails should have hover effect (slight scale or shadow).

D) PROJECT FILTER PILL STYLING
- Find the Projects page filter UI (All/Games/Software).
- Increase:
  - font size to base or slightly above
  - padding to px-4 py-2 (or similar)
  - rounded to rounded-full or rounded-lg
- Active pill:
  - bg-blue-600 text-white
- Inactive:
  - bg-gray-100 text-gray-700 hover:bg-gray-200
- Add gap-2 or gap-3 between pills.

E) HOME BUTTON (View My Work)
- Find the Home page CTA button.
- Change to:
  - bg-gray-700 text-white hover:bg-gray-800
  - keep padding and radius consistent

REQUIREMENTS
- Do NOT break existing routes or components.
- Prefer minimal new dependencies (use plain React state for modal).
- Update TypeScript types so the build passes.
- After changes, verify:
  - Projects page filter pills look bigger
  - Home CTA is grey
  - Project detail pages show Media section without needing further edits.

Finally, list the exact files you changed and what you changed in each.
